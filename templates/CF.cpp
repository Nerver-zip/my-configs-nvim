#include <bits/stdc++.h>
using namespace std;
using ll = long long;

static const auto fastio = []() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}();

// ==================== Debug ====================

#ifdef LOCAL
    #define dbg(x) cerr << #x << " = " << (x) << '\n'
#else
    #define dbg(x)
#endif

#ifdef LOCAL
    static const auto fileio = [](){
        freopen("../input.txt", "r", stdin);
        freopen("../output.txt", "w", stdout);
        return 0;
    }();
#endif

int main() {

    
    
    return 0;
}
